# Methods are essentially function that are built into objects
# For getting available methods press myList.(tab)
# Jupter notebook : for knowing more about the method myList.insert(shift + tab) 
# without jupyter notebook : help(myList.insert)

# for example
myList = [1, 2, 3, 4]
myList.append(5)
myList.pop()

# The secret module is used for generating cryptographycally rendom numbers suitable for managing data such as
# password, account authentication, security token and so on
